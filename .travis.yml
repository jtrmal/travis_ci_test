# Enable C++ support
language: cpp
sudo: required
dist: trusty

# Compiler selection
compiler:
  - clang

addons:
  apt:
    sources:
      - llvm-toolchain-trusty-3.9
    packages:
      - clang

# Build steps
script:
  - mkdir build
  - cd build
  - clang --version


